export const defaults = Object.freeze({
  nodeEnv: 'development',
  botName: 'SkyVerse',
  prefix: '.',
  logLevel: 'info',
  authPath: './sessions/whatsapp',
  whatsappLogLevel: 'silent',
  autoRead: true,
  autoOnline: true,
});
